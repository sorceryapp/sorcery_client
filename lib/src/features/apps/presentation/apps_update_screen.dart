import 'package:flutter/material.dart';

class AppsUpdateScreen extends StatelessWidget {
  const AppsUpdateScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
      'App Update Screen',
      style: TextStyle(fontSize: 20),
    );
  }
}
